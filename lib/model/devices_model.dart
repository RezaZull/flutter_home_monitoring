class DevicesModel {
  String deviceName;
  bool deviceStatus; //on off
  String deviceImage;

  DevicesModel(
      {required this.deviceName,
      required this.deviceStatus,
      required this.deviceImage});
}
